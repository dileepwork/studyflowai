{
    "name": "studyflowai-root",
    "private": true,
    "scripts": {
        "install": "cd frontend && npm install",
        "build": "cd frontend && npm run build && cd .. && mkdir dist && cp -r frontend/dist/* dist/"
    }
}